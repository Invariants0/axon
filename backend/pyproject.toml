[project]
name = "axon-backend"
version = "0.1.0"
description = "AXON FastAPI backend"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn>=0.30.0",
  "pydantic>=2.9.0"
]

[tool.uv]
package = false
